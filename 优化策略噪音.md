

## 改动总结

### 核心问题
BTC/XAU 的止损经常差一点到止盈价位就被扫出局，原因是：
1. **固定百分比止损**：trailing stop 用的是 `trailing_distance_pct: 0.008`（0.8%），不随市场波动率自适应
2. **波动率升高时止损过紧**：BTC M15 的 ATR 通常在 $150-$500 之间，0.8% 在 BTC $77,000 时只有 $616，约 1-2 倍 ATR，容易被噪音扫出

### 改了什么

**1. `src/mt5_quant/config.py`** — 新增 `atr_trailing_multiple` 字段
- 在 `StrategyConfig` dataclass 中增加了 `atr_trailing_multiple: float` 字段
- 加载时默认值为 `2.0`

**2. `src/mt5_quant/live.py`** — 移动止损从固定百分比改为 ATR 自适应
- 新增 `_compute_atr()` 静态方法，从实时 K 线计算当前 ATR
- `_apply_trailing_stop()` 改为：`止损距离 = 当前ATR × atr_trailing_multiple`
- 市场波动变大时，ATR 自动升高，止损距离自动拉大；波动收窄时，止损自动收紧
- 保留回退机制：如果 ATR 计算失败，使用价格 × 0.001 作为保底

**3. BTC M15 参数调优**（`config.btcusd.m15.yaml` + `profiles/btc.runtime.yaml`）
- `atr_stop_multiple`: **2.4 → 2.8**（入场止损更宽，减少被噪音触发）
- `reward_to_risk`: **2.6 → 2.8**（维持风险收益比）
- 新增 `atr_trailing_multiple: 2.5`（移动止损距离 = ATR × 2.5）

**4. XAU M15 参数调优**（`config.xauusd.m15.yaml`）
- `atr_stop_multiple`: **2.0 → 2.4**（黄金波动大，需要更多空间）
- 新增 `atr_trailing_multiple: 2.5`

**5. 所有 XAU runtime profiles**（`profiles/xau.runtime.yaml`, `xau_m1.runtime.yaml`, `xau_m5.runtime.yaml`）
- `atr_stop_multiple`: 2.0 → 2.4
- 新增 `atr_trailing_multiple: 2.5`

**6. EMA Cross ATR 策略**（`config.ema_cross_atr.yaml`）
- `atr_stop_multiple`: 2.2 → 2.6
- 新增 `atr_trailing_multiple: 2.5`

### 效果对比示例
| 场景 | BTC ATR (M15) | 旧止损距离 | 新止损距离 |
|------|---------------|-----------|-----------|
| 平静期 | $200 | $616 (0.8% 固定) | $500 (2.5×ATR) |
| 正常波动 | $350 | $616 | $875 |
| 高波动 | $500 | $616 | $1,250 |
| 极端波动 | $800 | $616 | $2,000 |

新机制确保止损始终与当前市场状态匹配，不会在波动放大时被噪音扫出。